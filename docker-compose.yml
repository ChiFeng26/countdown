version: "3.9"

services:
  countdown:
    image: nginx:alpine
    container_name: countdown
    ports:
      - "8080:80"
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro
    restart: unless-stopped
